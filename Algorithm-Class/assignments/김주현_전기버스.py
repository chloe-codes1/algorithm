

T = int(input())

for t in range(1,T+1):














    
    # K, N, M = input().split()
    # K = int(K)
    # N = int(N)
    # M = int(M)
    
    # chargable = list(map(int, input().split()))

    # count = 0
    # position = 0
    # fuel = K
    # while True:
        
    #     fuel -= 1
    #     position +=1
    
    #     if position in chargable:
    #         if fuel < K and N > position + fuel:
    #             print('position & fuel', position, fuel)
    #             fuel = K
    #             count += 1
        
    #     if position == N:
    #         break


    #     if fuel ==0 and position < N:
    #         count = 0
    #         break
    
    # print('#{} {}'.format(t, count))
            
